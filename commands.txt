pip install virtualenv
virtualenv env
env\Scripts\activate
pip install gunicorn
pip3 freeze > requirements.txt
git init
git add .
git commit -m "Init flask app"

#Check the 
(env) PS D:\Python_projects\FlaskProduction> git remote -v
origin  https://github.com/ipkakiu/flaskapp_todo_list (fetch)
origin  https://github.com/ipkakiu/flaskapp_todo_list (push)
git remote add origin https://github.com/ipkakiu/flaskapp_todo_list

need to create repository on github.com if using git init instead of directly publishing the folder to github

